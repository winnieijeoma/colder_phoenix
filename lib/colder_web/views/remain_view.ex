defmodule ColderWeb.RemainView do
  use ColderWeb, :view

  # def fullname(id) do
  #   remain = 
  #   "#{remain.firstname} #{remain.middlename} #{remain.surname}"
  # end
  
  # <span class="inline pr-2"><%= fullname(@remain.id) %></span>
end
